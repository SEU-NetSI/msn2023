1.压缩包中包 msn2022网站的代码
2.需要下载xampp（https://www.apachefriends.org/）来帮助进行配置
    - 安装完成xampp之后，找到documentroot字段，设置成网站文件夹路径，启动xampp，即可在本地localhost访问
3.主文件夹中，每一个php文件代表着一个页面
    - index.php 主页面
    - registration 注册页面
4.include文件夹中包含了每个页面都有的组件，例如页脚(footer)，页眉(header)，导航(navbar)，页边日期(dates)等